version: "3.9"

services:
  api:
    build: .
    container_name: trading-api
    volumes:
      - ./app:/app/app
      - ./trades.db:/app/trades.db
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
