# Base defaults shared by all profiles (optional)
# Keep non-secret values only.

# APP_ENV is selected at runtime. Valid values: dev, stage, prod
APP_ENV=dev

# Use this only if you want a global DB override for all profiles.
# Profile-specific files override this value.
DATABASE_URL=sqlite+aiosqlite:///./data/trades.db
